server:
  port: 8080
  mode: debug

database:
  host: localhost
  port: 5432
  user: postgres
  password: postgres
  dbname: bo_patrol
  sslmode: disable

jwt:
  secret: bo-patrol-jwt-secret-2024
  expires: 720

redis:
  host: localhost
  port: 6379
  password: ""
  db: 0

minio:
  endpoint: localhost:9000
  accessKey: minioadmin
  secretKey: minioadmin
  bucket: patrol-photos
  useSSL: false

log:
  level: info
  format: json
